
import { textLimit } from 'my-app/helpers/text-limit';
import { module, test } from 'qunit';

module('Unit | Helper | text limit');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = textLimit([42]);
  assert.ok(result);
});

